# HiveMind — Python 3.14
# Core (the server does not run without these)
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
rich>=13.7

# Windows-specific (live VRAM query, desktop notifications)
pywin32>=306; sys_platform == "win32"
watchfiles>=0.21
# Desktop notifications (optional: settings["desktop_notifications"]=false
# turns toasts off; if the package is missing, it degrades to logging)
winotify>=0.1.5; sys_platform == "win32"

# Recommended (features degrade gracefully without these packages)
trafilatura>=1.12          # websearch: full-text extraction
tree-sitter-language-pack>=0.2   # static repo-map (tree-sitter parser)
pyyaml>=6.0                # skills frontmatter

# Optional (only for browser_tool; afterwards: playwright install chromium)
# playwright>=1.45
